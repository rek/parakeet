# training-engine

the reason we have this engine is...

## Building

Run `nx build training-engine` to build the library.

## Running unit tests

Run `nx test training-engine` to execute the unit tests via [Jest](https://jestjs.io).
