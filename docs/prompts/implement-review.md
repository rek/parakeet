We have just done a full review and added 'Review Findings' sections to the bottom of many existing `docs/features/*/design*.md` and `docs/features/*/spec-*.md` files.

Now we need to systematically work though implementing them all and incorporating those findings into the design docs main content in the correct places, so the docs read as comprehensive, not with addendums.
