# mobile-050: Video Analysis Calibration Tests

**Status:** Done
**Depends on:** mobile-046 (video form analysis), mobile-049 (additional video metrics)

## What This Covers

End-to-end calibration tests that run the video analysis pipeline against real stock footage and validate outputs against a manifest of expected results. The existing unit tests (92 Vitest) use synthetic sine-wave landmark data. These tests close the gap: they verify the full pipeline produces sensible metrics on actual gym video, and they create regression anchors so future algorithm changes don't silently break.

Six royalty-free Pexels videos are already downloaded in `test-videos/`. A `manifest.json` describes each video's metadata and expected outputs. The tests run the analysis pipeline (frames → landmarks → metrics), compare results to the manifest, and fail on regressions once calibrated.

## Background

### Test Videos (`test-videos/`)

| ID | File | Lift | Orientation | Duration | Primary test purpose |
|---|---|---|---|---|---|
| `squat-side-clean` | squat-side-clean.mp4 | squat | landscape | 20.5s | Baseline squat — depth, lean, bar path |
| `squat-front-angle` | squat-front-angle.mp4 | squat | landscape | 28.3s | Camera angle auto-detection |
| `squat-portrait` | squat-gym-workout.mp4 | squat | portrait | 27.4s | Portrait orientation handling |
| `deadlift-side` | deadlift-barbell.mp4 | deadlift | portrait | 15.4s | Back rounding, lockout detection |
| `deadlift-back` | deadlift-back-workout.mp4 | deadlift | landscape | 13.6s | Cross-validation, different conditions |
| `bench-press` | bench-press.mp4 | bench | landscape | 15.7s | Bar path, touch-point variance |

All videos are Pexels royalty-free (no attribution required). `.mp4` files are gitignored; `manifest.json` is tracked.

### Manifest Structure (`test-videos/manifest.json`)

Each video entry has:
- `expected` — predicted camera angle, rep count range, which faults/metrics to check
- `actual` — `null` until populated by a calibration run
- `calibrated` — `false` until a human verifies `actual` values visually

### Pipeline Under Test

```
video file → extractFramesFromVideo(uri, duration, 4fps)
           → { frames: PoseFrame[], fps }
           → analyzeVideoFrames(frames, fps, lift)
             → interpolateEmptyFrames()
             → assembleAnalysis()
               → bar path extraction (wrist landmarks)
               → rep detection (Y-periodicity peak finding)
               → per-rep: angles, depth, drift, velocity, faults, grading
           → VideoAnalysisResult { reps[], fps, sagittalConfidence, analysisVersion }
```

Key entry points in `modules/video-analysis/`:
- `application/analyze-video.ts` — `extractFramesFromVideo()`, `analyzeVideoFrames()`
- `lib/metrics-assembler.ts` — `assembleAnalysis()` (analysisVersion 3)
- `lib/fault-detector.ts` — thresholds and fault detection
- `lib/bar-path.ts`, `lib/angle-calculator.ts`, `lib/rep-detector.ts`, `lib/depth-detector.ts`, `lib/bar-velocity.ts`, `lib/rep-tempo.ts`
- `lib/fatigue-signatures.ts` — cross-rep fatigue analysis
- `lib/butt-wink-detector.ts`, `lib/stance-width.ts`, `lib/hip-shift.ts` — squat-specific
- `lib/elbow-flare.ts`, `lib/pause-quality.ts` — bench-specific
- `lib/hip-hinge-timing.ts`, `lib/bar-shin-distance.ts` — deadlift-specific
- `lib/lockout-stability.ts` — all lifts

## Tasks

### Phase 1 — Landmark Extraction Script (Device-Free)

**`scripts/extract-landmarks.py`:**

- [x] Python script that extracts MediaPipe PoseLandmarker (Lite model) landmarks from test videos
  - Uses `mediapipe` + `opencv-python` (no React Native / device dependency)
  - Reads `test-videos/manifest.json`, processes each video at 4fps
  - Downloads `pose_landmarker_lite.task` model automatically on first run
  - Outputs `test-videos/landmarks/<video-id>.landmarks.json` per video
  - Supports `--force` flag to re-extract
  - Prints summary table with detection rates
- [x] Landmark JSON fixtures committed to git (50-230KB each, ~960KB total)
  - Videos are gitignored; landmarks are tracked so CI can run tests without videos
- [x] npm script `"extract:landmarks"` added to root `package.json`

### Phase 2 — Vitest Integration Tests

**`modules/video-analysis/lib/__tests__/calibration.test.ts`:**

These tests use **pre-extracted landmark data** (JSON fixtures) from the calibration runner. This lets the algorithm tests run in CI without MediaPipe native deps.

- [x] `test-videos/landmarks/` directory with 6 `.landmarks.json` files (generated by Phase 1 Python script, committed to git)
- [x] `describe.each(manifest.videos)` driven by manifest — all 6 videos tested via one parameterized block
- [x] `describe('calibration: squat-side-clean')`:
  - Loads `squat-side-clean.landmarks.json`
  - Calls `assembleAnalysis({ frames, fps, lift: 'squat' })`
  - Asserts: `result.sagittalConfidence > 0.7` (high confidence for side-view video)
  - Asserts: `result.reps.length` within manifest's `expected.rep_count` range
  - Asserts: every rep has `forwardLeanDeg`, `maxDepthCm`, `barDriftCm` defined (not null)
  - Asserts: squat metrics present — `stanceWidthCm`, `hipShiftCm`, `hipShiftDirection`, `lockoutStabilityCv`
  - Asserts: `buttWinkDeg` absent on clean reps, present on wink reps
  - Asserts: `result.fatigueSignatures` present when ≥2 reps
  - Asserts: no unexpected `critical` faults on a clean lift (or expected faults if form is bad)
  - Once calibrated: snapshot key metric values with ±10% tolerance
- [x] `describe('calibration: squat-front-angle')`:
  - Asserts `result.sagittalConfidence` within manifest's expected range (e.g., `< 0.3` for front-view videos)
  - All metrics always computed regardless of angle — verify `kneeValgus`, `hipShiftCm`, `stanceWidthCm`, standard squat metrics all present
  - Asserts: `stanceWidthCm` > 0 (should always be computable)
- [x] `describe('calibration: squat-portrait')`:
  - Same metric assertions as squat-side-clean
  - Key test: portrait aspect ratio doesn't degrade landmark quality
  - Compare rep count and metric ranges to squat-side-clean — should be similar order of magnitude
  - Asserts: squat-specific metrics (butt wink, stance, hip shift) produce values
- [x] `describe('calibration: deadlift-side')`:
  - Calls `assembleAnalysis({ frames, fps, lift: 'deadlift' })`
  - Asserts: `hipAngleAtLockoutDeg` is present on every rep
  - Asserts: `barDriftCm` is present
  - Asserts: deadlift metrics present — `hipHingeCrossoverPct`, `barToShinDistanceCm`, `lockoutStabilityCv`
  - Asserts: `hipHingeCrossoverPct` is between 0 and 100 on every rep
  - Checks `back_rounding`, `incomplete_lockout`, `early_hip_shoot`, `bar_away_from_shins` fault detection
  - Asserts: `result.fatigueSignatures` present when ≥2 reps
- [x] `describe('calibration: deadlift-back')`:
  - Cross-validates against deadlift-side — metric ranges should be in the same ballpark
  - Different filming conditions shouldn't cause wildly different outputs
  - Asserts: same deadlift-specific metrics present
- [x] `describe('calibration: bench-press')`:
  - Calls `assembleAnalysis({ frames, fps, lift: 'bench' })`
  - Asserts: bar path tracks wrist Y correctly (ROM should be positive, reasonable range)
  - Asserts: `barDriftCm` is present
  - Asserts: bench metrics present — `elbowFlareDeg`, `pauseDurationSec`, `isSinking`, `lockoutStabilityCv`
  - Asserts: `elbowFlareDeg` between 0° and 90° (sanity check)
  - Asserts: `pauseDurationSec` ≥ 0
  - If 2+ reps: `computeTouchPointVariance()` returns a value, `result.fatigueSignatures` present

### Phase 3 — Regression Snapshots

- [x] All 15 videos calibrated — `manifest.json` updated with `calibrated: true` and exact `actual` values via `calibrate-videos.ts`
- [x] `describe('regression')` block implemented — activates when `calibrated: true` in manifest
- [x] Tolerance-based snapshot comparison (`snapshot-compare.ts`) asserts metrics match within tolerance:
  - Rep count: exact match
  - Angles (forwardLean, kneeAngle, hipAngle, elbowFlare, buttWink): ±5°
  - Distances (depth, ROM, drift, stanceWidth, hipShift, barToShin): ±2cm
  - Velocity: ±10%
  - Timing (pauseDuration, hipHingeCrossover): ±10%
  - Stability (lockoutStabilityCv): ±1% absolute
  - Fatigue signatures: ±15% or ±1 absolute (whichever is larger)
  - Faults: exact type set match (same fault types fire on same reps)
  - Verdicts: exact match with presence mismatch detection
- [x] Regression tests gate future algorithm changes — 185 calibration tests (15 videos × 4 regression checks + per-video assertions)

### Phase 4 — Manifest Update Automation

- [x] `scripts/calibrate-videos.ts` — runs all test videos through pipeline, generates snapshot files (`test-videos/snapshots/`), updates manifest `actual` fields. Flags: `--video`, `--update-manifest`, `--mark-calibrated`, `--dry-run`, `--force`
- [x] NPM scripts: `calibrate:videos` (inspect + update manifest), `calibrate:snapshots` (full re-calibration with force overwrite)
- [x] `test-videos/README.md` — documents directory structure, adding new videos, calibration workflow, regression testing

## Key Thresholds (from `fault-detector.ts` + `metrics-assembler.ts`)

These are the thresholds the tests should validate against:

| Fault | Lift | Threshold | Severity |
|---|---|---|---|
| `above_parallel` | squat | hip Y ≤ knee Y at bottom | critical |
| `excessive_lean` | squat | forward lean > 55° | warning |
| `bar_drift` | all | drift > 0.03 normalized (~7.3cm) | warning |
| `back_rounding` | deadlift | hip angle deviation > 15° mid-pull | warning |
| `incomplete_lockout` | deadlift | hip angle < 170° at top | info |
| `butt_wink` | squat | hip angle drop > 10° in < 200ms at bottom | warning |
| `elbow_flare` | bench | flare > 80° or < 30° | warning |
| `early_hip_shoot` | deadlift | hip velocity crossover < 30% of pull | warning |
| `bar_away_from_shins` | deadlift | wrist-knee distance > 5cm | warning |
| `unstable_lockout` | all | hip angle CV > 5% in last 10% of rep | warning |

## Impact from mobile-052 (View Angle Rework)

Post mobile-052 calibration updates (completed):

- **Camera angle assertion:** Tests assert `sagittalConfidence` numeric ranges (e.g., `> 0.7` for side-view videos, `< 0.3` for front-view) instead of binary string match.
- **Metric presence:** All metrics always computed regardless of view angle. No `if (camera_angle === 'side')` guards — every video produces every metric for its lift.
- **Rep detection:** Joint-angle-based detection (viewpoint-invariant) replaced Y-coordinate method. Manifest `rep_count` ranges may need tightening for 45° videos.
- **New validation:** Per-metric confidence scores asserted (high confidence on side-view videos, lower on front-view).

The 45° angle test videos (`squat-45-3reps`, `deadlift-45-6reps`, `bench-45-5reps`) are the most important regression anchors — they exercise the confidence-weighted code paths.

## Dependencies

- [mobile-046-video-form-analysis.md](./mobile-046-video-form-analysis.md) — the pipeline being tested
- [mobile-049-additional-video-metrics.md](./mobile-049-additional-video-metrics.md) — additional metrics (fatigue, squat/bench/deadlift-specific, lockout)
- [mobile-052-view-angle-rework.md](./mobile-052-view-angle-rework.md) — changes camera angle detection, rep detection, and metric gating (requires test updates)
- Test videos in `test-videos/` (already downloaded, `.mp4` files gitignored)
